-extraDoc = "For each of the processes you would like to run add the matching configuration by adding a new process configuration below and type in your configuration. Example configuration for supported registries and registrars can be found at <a href='http://documentation.atomia.com/DomainRegistration/latest/html/configexamples.html#configexamples-examples' target='#'>http://documentation.atomia.com/DomainRegistration/latest/html/configexamples.html#configexamples-examples</a>"

-c = config["domainreg_tld_config_hash"]
label(for='#{c.name}')
  =c.name
&nbsp;<i>(#{c.doc})</i>
p
  | !{extraDoc}

script.
  $(document).ready(function(){
    domainregLoad(!{JSON.stringify(c)});
  });


label(for='processList')
  | Process list (All processes to be added, click to edit)
ul#processList
  li#noProcess No processes added yet

form.form-horizontal
  fieldset
    legend Add TLD processes
  div.row
    div.form-group
      label.col-lg-1.control-label(for='domainreg_tld_config_hash_name') Process Name
      div.col-lg-8
        input.form-control(id="domainreg_tld_config_hash_name", class="", type="text", value="", name="domainreg_tld_config_hash_name")
        span.help-block Name (Can be anything)
  div.row
    div.form-group
      label.col-lg-1.control-label(for='domainreg_tld_config_hash') TLD configuration
      div.col-lg-8
        textarea.form-control(id="domainreg_tld_config_hash", class="", name="domainreg_tld_config_hash", rows="15")
        span.help-block Configuration (TLD configuration based on <a href='http://documentation.atomia.com/DomainRegistration/latest/html/configexamples.html#configexamples-examples' target='#'>configuration examples</a>. One process per configuration box!)
        <br>
        button.btn.btn-s.btn-info#addTLD(type="submit",onClick="addTLDProcess();return false;") Save TLD process
        div#tld_processes
        <br>
